<section>
    <h1>Categories</h1>

    <nav>
        <ul>
            {% for entity in entities %}
                <li>
                   <a href="{{ path('category_show', { 'id': entity.id }) }}">{{ entity.indentedName }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
</section>